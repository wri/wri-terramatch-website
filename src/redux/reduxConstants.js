/**
 * Constants for use in async flux standard actions
 * @const
 */
const ReduxConstants = Object.freeze({
  PENDING_SUFFIX: "pending",
  SUCCESS_SUFFIX: "succ",
  FAILURE_SUFFIX: "fail",
  INVALIDATE_SUFFIX: "invalidate",
  CLEAR_SUFFIX: "clear"
});

export default ReduxConstants;
