version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install `cat .nvmrc`
        - nvm use
        - yarn install
    build:
      commands:
        - export NODE_OPTIONS=--max-old-space-size=6144
        - yarn run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*
